{% extends 'base.html.twig' %}

{% block title %}{{ 'title.thread_edit'|trans }}: {{ thread.title }}{% endblock %}

{% block body %}
    <div class="container page_wrapper">
        <h1 class="page_title heading_primary">{{ 'heading.thread_edit'|trans }}</h1>

        {{ form_start(form, {'attr': {'class': 'form_wrapper'}}) }}
        <div class="form_content_wrapper">
            {{ form_widget(form) }}
        </div>

        <div class="action_buttons_wrapper margin_top_3">
            <button type="submit" class="button button_primary">{{ 'action.update'|trans }}</button>
            <a href="{{ path('thread_index') }}" class="button button_secondary">{{ 'action.back_to_list'|trans }}</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
